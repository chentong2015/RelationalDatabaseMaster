使用explain和profile分析查询语句
在千万级分页时使用limit，开缓存常用查询

count(Field)替代count(*)提高统计速度，避免使用*查询
先对表where筛选之后再join联合查询，小表驱动(JOIN)大表

distinct 不能作用到BLOB字段，无法针对二进制大数据进行比对
where 条件单表查，锁定最小返回记录表
order by limit 形式的sql语句让排序的表优先查
select distinct user2 from table 对于千万级数据表可能造成性能问题